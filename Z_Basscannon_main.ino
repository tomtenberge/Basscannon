void setup() {
  Serial.begin(115200);
}
void loop() {
  //Read out analog pin 7 for audio volumes
  //read_audio();
  //audio_serialize();
  delay(10);        // delay in between reads for stability
}
